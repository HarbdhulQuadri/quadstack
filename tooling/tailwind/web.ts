// Tailwind CSS v4 is configured via CSS, not JS config files.
// This file exists as a compatibility shim for tools that require a JS/TS config.
// Your actual theme lives in packages/ui/src/globals.css as CSS custom properties.
//
// If you need Tailwind v3-style JS config (e.g. for a tool that doesn't support v4):
//   import type { Config } from "tailwindcss";
//   export default { content: [], theme: { extend: {} }, plugins: [] } satisfies Config;

export default {};
